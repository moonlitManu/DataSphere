spring:
  application:
    name: DataSphere
  datasource:
    url: jdbc:mariadb://localhost:3306/datasphere
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: 
emailService:
  email:
  appPassword: 
